# Local dev: leave empty so Vite proxies /api to http://127.0.0.1:5050
VITE_API_URL=

# Production (Netlify): https://auction-backend-9bgs.onrender.com
# Set in Netlify → Site settings → Environment variables, or use frontend/.env.production
